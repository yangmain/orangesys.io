machine:
  node:
    version: 6.9.1
  timezone:
    Asia/Tokyo
  environment:
    GOPATH: "/home/ubuntu/.go_workspace"
    GODIST: "go1.7.4.linux-amd64.tar.gz"
    PROJECT_NAME: www-orangesys-io
    CLOUDSDK_COMPUTE_ZONE: asia-northeast1-c
    SHORT_HASH: $(echo $CIRCLE_SHA1 | cut -c -7)
  post:
    - mkdir -p download
    - test -e download/$GODIST || curl -o download/$GODIST https://storage.googleapis.com/golang/$GODIST
    - sudo rm -rf /usr/local/go
    - sudo tar -C /usr/local -xzf download/$GODIST
    - go get -v github.com/spf13/hugo

dependencies:
  pre:
    - hugo version
    - git tag
    - npm install -g gulp
    - npm install
  override:
    - /bin/true

test:
  override:
    - gulp build:prod

deployment:
  release:
    branch: release
    commands:
      - sudo /opt/google-cloud-sdk/bin/gcloud --quiet components update
      - echo $GCLOUD_SERVICE_KEY | base64 --decode -i > ${HOME}/account-auth.json
      - sudo /opt/google-cloud-sdk/bin/gcloud config set project $PROJECT_NAME
      - sudo /opt/google-cloud-sdk/bin/gcloud auth activate-service-account --key-file ${HOME}/account-auth.json
      - sudo /opt/google-cloud-sdk/bin/gcloud -q app deploy app.yaml --promote --version ${SHORT_HASH}
